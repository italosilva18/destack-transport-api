# Configurações para ambiente Docker

# API
ENVIRONMENT=production
SERVER_PORT=8080

# Banco de dados PostgreSQL
DB_HOST=postgres
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=destack_secure_password_2024
DB_NAME=destack_transport
DB_SSLMODE=disable

# JWT
JWT_SECRET=destack_jwt_secret_key_production_2024_min_32_chars
JWT_EXPIRES_IN=24

# PGAdmin (opcional)
PGADMIN_EMAIL=admin@destack.com
PGADMIN_PASSWORD=pgadmin_secure_password_2024

# Redis (opcional)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=redis_secure_password_2024

# Grafana (opcional)
GRAFANA_PASSWORD=grafana_secure_password_2024

# Email (futuro)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=noreply@destack.com
# SMTP_PASS=smtp_password
# SMTP_FROM=noreply@destack.com

# AWS S3 (futuro)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_REGION=us-east-1
# AWS_BUCKET=destack-uploads

# Sentry (futuro)
# SENTRY_DSN=https://your_sentry_dsn@sentry.io/project_id